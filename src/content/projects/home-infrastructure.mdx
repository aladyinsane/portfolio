---
title: "Home Infrastructure & Automation"
summary: "A self-hosted Docker stack for home automation, plus a Python bill-pay automation system — the same engineering habits applied to problems nobody assigned."
order: 3
kind: personal
stack: ["Docker", "Python", "Home Assistant", "Tailscale", "n8n"]
year: "2023–present"
draft: false
---

## What it is

Two related, ongoing projects, both solving problems for a household of one instead
of a client:

**A self-hosted home automation stack.** A Docker Compose stack running Mosquitto
(MQTT broker), ring-mqtt (bridges Ring devices onto MQTT), and Home Assistant, tied
together with Tailscale for secure remote access and n8n for workflow automation —
without exposing anything directly to the open internet.

**A bill-pay automation system.** A Python system that handles recurring bill logic
against an Excel-based ledger, so bill tracking is automated and auditable instead of
manual and scattered across statements.

## Approach

Same engineering instincts as the paid work, applied without anyone requiring it:
containerized services instead of ad hoc installs, a private mesh network instead of
open ports, workflow automation instead of manual steps, and a version-trackable
ledger instead of a shoebox of statements.

## Why it's here

Employers only see the systems thinking you were assigned to apply. This is evidence
it's not switched on and off with a badge — it's how I default to building things,
including the things nobody's paying me to build well.

{/* TODO(Lauren): add specifics once you walk me through it — what the n8n
   workflows actually automate, how many devices/integrations are in play, whether
   the repo is public and worth linking. */}
